# @meewmeew/meta runtime

Platform runtime package for `@meewmeew/meta`. This package is installed automatically for the matching operating system and CPU architecture and contains only the `meta-runtime` executable plus package metadata.

Do not depend on this package directly unless you specifically need the bundled runtime file contract exposed as `./runtime`.
