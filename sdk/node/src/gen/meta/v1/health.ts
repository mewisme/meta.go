// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: meta/v1/health.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp.js";

export const protobufPackage = "meta.v1";

export enum ConnectionState {
  CONNECTION_STATE_UNSPECIFIED = 0,
  CONNECTION_STATE_DISCONNECTED = 1,
  CONNECTION_STATE_CONNECTING = 2,
  CONNECTION_STATE_CONNECTED = 3,
  CONNECTION_STATE_FAILED = 4,
  UNRECOGNIZED = -1,
}

export function connectionStateFromJSON(object: any): ConnectionState {
  switch (object) {
    case 0:
    case "CONNECTION_STATE_UNSPECIFIED":
      return ConnectionState.CONNECTION_STATE_UNSPECIFIED;
    case 1:
    case "CONNECTION_STATE_DISCONNECTED":
      return ConnectionState.CONNECTION_STATE_DISCONNECTED;
    case 2:
    case "CONNECTION_STATE_CONNECTING":
      return ConnectionState.CONNECTION_STATE_CONNECTING;
    case 3:
    case "CONNECTION_STATE_CONNECTED":
      return ConnectionState.CONNECTION_STATE_CONNECTED;
    case 4:
    case "CONNECTION_STATE_FAILED":
      return ConnectionState.CONNECTION_STATE_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConnectionState.UNRECOGNIZED;
  }
}

export function connectionStateToJSON(object: ConnectionState): string {
  switch (object) {
    case ConnectionState.CONNECTION_STATE_UNSPECIFIED:
      return "CONNECTION_STATE_UNSPECIFIED";
    case ConnectionState.CONNECTION_STATE_DISCONNECTED:
      return "CONNECTION_STATE_DISCONNECTED";
    case ConnectionState.CONNECTION_STATE_CONNECTING:
      return "CONNECTION_STATE_CONNECTING";
    case ConnectionState.CONNECTION_STATE_CONNECTED:
      return "CONNECTION_STATE_CONNECTED";
    case ConnectionState.CONNECTION_STATE_FAILED:
      return "CONNECTION_STATE_FAILED";
    case ConnectionState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface HealthSnapshot {
  regular: ConnectionState;
  e2ee: ConnectionState;
  reconnectCount: bigint;
  engineDroppedEventCount: bigint;
  subscriberDroppedEventCount: bigint;
  lastSuccessfulSend: Date | undefined;
  lastReceive: Date | undefined;
  lastErrorCategory: string;
}

function createBaseHealthSnapshot(): HealthSnapshot {
  return {
    regular: 0,
    e2ee: 0,
    reconnectCount: 0n,
    engineDroppedEventCount: 0n,
    subscriberDroppedEventCount: 0n,
    lastSuccessfulSend: undefined,
    lastReceive: undefined,
    lastErrorCategory: "",
  };
}

export const HealthSnapshot: MessageFns<HealthSnapshot> = {
  encode(message: HealthSnapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.regular !== 0) {
      writer.uint32(8).int32(message.regular);
    }
    if (message.e2ee !== 0) {
      writer.uint32(16).int32(message.e2ee);
    }
    if (message.reconnectCount !== 0n) {
      if (BigInt.asUintN(64, message.reconnectCount) !== message.reconnectCount) {
        throw new globalThis.Error("value provided for field message.reconnectCount of type uint64 too large");
      }
      writer.uint32(24).uint64(message.reconnectCount);
    }
    if (message.engineDroppedEventCount !== 0n) {
      if (BigInt.asUintN(64, message.engineDroppedEventCount) !== message.engineDroppedEventCount) {
        throw new globalThis.Error("value provided for field message.engineDroppedEventCount of type uint64 too large");
      }
      writer.uint32(32).uint64(message.engineDroppedEventCount);
    }
    if (message.subscriberDroppedEventCount !== 0n) {
      if (BigInt.asUintN(64, message.subscriberDroppedEventCount) !== message.subscriberDroppedEventCount) {
        throw new globalThis.Error(
          "value provided for field message.subscriberDroppedEventCount of type uint64 too large",
        );
      }
      writer.uint32(40).uint64(message.subscriberDroppedEventCount);
    }
    if (message.lastSuccessfulSend !== undefined) {
      Timestamp.encode(toTimestamp(message.lastSuccessfulSend), writer.uint32(50).fork()).join();
    }
    if (message.lastReceive !== undefined) {
      Timestamp.encode(toTimestamp(message.lastReceive), writer.uint32(58).fork()).join();
    }
    if (message.lastErrorCategory !== "") {
      writer.uint32(66).string(message.lastErrorCategory);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthSnapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseHealthSnapshot();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.regular = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.e2ee = reader.int32() as any;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.reconnectCount = reader.uint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.engineDroppedEventCount = reader.uint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.subscriberDroppedEventCount = reader.uint64() as bigint;
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.lastSuccessfulSend = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.lastReceive = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.lastErrorCategory = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): HealthSnapshot {
    return {
      regular: isSet(object.regular) ? connectionStateFromJSON(object.regular) : 0,
      e2ee: isSet(object.e2ee) ? connectionStateFromJSON(object.e2ee) : 0,
      reconnectCount: isSet(object.reconnectCount)
        ? BigInt(object.reconnectCount)
        : isSet(object.reconnect_count)
        ? BigInt(object.reconnect_count)
        : 0n,
      engineDroppedEventCount: isSet(object.engineDroppedEventCount)
        ? BigInt(object.engineDroppedEventCount)
        : isSet(object.engine_dropped_event_count)
        ? BigInt(object.engine_dropped_event_count)
        : 0n,
      subscriberDroppedEventCount: isSet(object.subscriberDroppedEventCount)
        ? BigInt(object.subscriberDroppedEventCount)
        : isSet(object.subscriber_dropped_event_count)
        ? BigInt(object.subscriber_dropped_event_count)
        : 0n,
      lastSuccessfulSend: isSet(object.lastSuccessfulSend)
        ? fromJsonTimestamp(object.lastSuccessfulSend)
        : isSet(object.last_successful_send)
        ? fromJsonTimestamp(object.last_successful_send)
        : undefined,
      lastReceive: isSet(object.lastReceive)
        ? fromJsonTimestamp(object.lastReceive)
        : isSet(object.last_receive)
        ? fromJsonTimestamp(object.last_receive)
        : undefined,
      lastErrorCategory: isSet(object.lastErrorCategory)
        ? globalThis.String(object.lastErrorCategory)
        : isSet(object.last_error_category)
        ? globalThis.String(object.last_error_category)
        : "",
    };
  },

  toJSON(message: HealthSnapshot): unknown {
    const obj: any = {};
    if (message.regular !== 0) {
      obj.regular = connectionStateToJSON(message.regular);
    }
    if (message.e2ee !== 0) {
      obj.e2ee = connectionStateToJSON(message.e2ee);
    }
    if (message.reconnectCount !== 0n) {
      obj.reconnectCount = message.reconnectCount.toString();
    }
    if (message.engineDroppedEventCount !== 0n) {
      obj.engineDroppedEventCount = message.engineDroppedEventCount.toString();
    }
    if (message.subscriberDroppedEventCount !== 0n) {
      obj.subscriberDroppedEventCount = message.subscriberDroppedEventCount.toString();
    }
    if (message.lastSuccessfulSend !== undefined) {
      obj.lastSuccessfulSend = message.lastSuccessfulSend.toISOString();
    }
    if (message.lastReceive !== undefined) {
      obj.lastReceive = message.lastReceive.toISOString();
    }
    if (message.lastErrorCategory !== "") {
      obj.lastErrorCategory = message.lastErrorCategory;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthSnapshot>): HealthSnapshot {
    return HealthSnapshot.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthSnapshot>): HealthSnapshot {
    const message = createBaseHealthSnapshot();
    message.regular = object.regular ?? 0;
    message.e2ee = object.e2ee ?? 0;
    message.reconnectCount = (object.reconnectCount !== undefined && object.reconnectCount !== null)
      ? BigInt(object.reconnectCount)
      : 0n;
    message.engineDroppedEventCount =
      (object.engineDroppedEventCount !== undefined && object.engineDroppedEventCount !== null)
        ? BigInt(object.engineDroppedEventCount)
        : 0n;
    message.subscriberDroppedEventCount =
      (object.subscriberDroppedEventCount !== undefined && object.subscriberDroppedEventCount !== null)
        ? BigInt(object.subscriberDroppedEventCount)
        : 0n;
    message.lastSuccessfulSend = object.lastSuccessfulSend ?? undefined;
    message.lastReceive = object.lastReceive ?? undefined;
    message.lastErrorCategory = object.lastErrorCategory ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
