// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: meta/v1/session.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Duration } from "../../google/protobuf/duration.js";
import { Account } from "./common.js";
import { Event } from "./events.js";
import { HealthSnapshot } from "./health.js";

export const protobufPackage = "meta.v1";

export interface CreateSessionRequest {
  cookies: { [key: string]: string };
  e2ee: boolean;
  eventBuffer: number;
  timeout: Duration | undefined;
}

export interface CreateSessionRequest_CookiesEntry {
  key: string;
  value: string;
}

export interface CreateSessionResponse {
  sessionId: string;
}

export interface ConnectRequest {
  sessionId: string;
}

export interface ConnectResponse {
  account: Account | undefined;
}

export interface CloseSessionRequest {
  sessionId: string;
}

export interface CloseSessionResponse {
}

export interface GetHealthRequest {
  sessionId: string;
}

export interface GetHealthResponse {
  health: HealthSnapshot | undefined;
}

export interface SubscribeEventsRequest {
  sessionId: string;
  buffer: number;
}

export interface SubscribeEventsResponse {
  event: Event | undefined;
}

function createBaseCreateSessionRequest(): CreateSessionRequest {
  return { cookies: {}, e2ee: false, eventBuffer: 0, timeout: undefined };
}

export const CreateSessionRequest: MessageFns<CreateSessionRequest> = {
  encode(message: CreateSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.cookies).forEach(([key, value]: [string, string]) => {
      CreateSessionRequest_CookiesEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    if (message.e2ee !== false) {
      writer.uint32(16).bool(message.e2ee);
    }
    if (message.eventBuffer !== 0) {
      writer.uint32(24).uint32(message.eventBuffer);
    }
    if (message.timeout !== undefined) {
      Duration.encode(message.timeout, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCreateSessionRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            const entry1 = CreateSessionRequest_CookiesEntry.decode(reader, reader.uint32());
            if (entry1.value !== undefined) {
              message.cookies[entry1.key] = entry1.value;
            }
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.e2ee = reader.bool();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.eventBuffer = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.timeout = Duration.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CreateSessionRequest {
    return {
      cookies: isObject(object.cookies)
        ? (globalThis.Object.entries(object.cookies) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            globalThis.Object.defineProperty(acc, key, {
              value: globalThis.String(value),
              enumerable: true,
              configurable: true,
              writable: true,
            });
            return acc;
          },
          {},
        )
        : {},
      e2ee: isSet(object.e2ee) ? globalThis.Boolean(object.e2ee) : false,
      eventBuffer: isSet(object.eventBuffer)
        ? globalThis.Number(object.eventBuffer)
        : isSet(object.event_buffer)
        ? globalThis.Number(object.event_buffer)
        : 0,
      timeout: isSet(object.timeout) ? Duration.fromJSON(object.timeout) : undefined,
    };
  },

  toJSON(message: CreateSessionRequest): unknown {
    const obj: any = {};
    if (message.cookies) {
      const entries = globalThis.Object.entries(message.cookies) as [string, string][];
      if (entries.length > 0) {
        obj.cookies = {};
        entries.forEach(([k, v]) => {
          obj.cookies[k] = v;
        });
      }
    }
    if (message.e2ee !== false) {
      obj.e2ee = message.e2ee;
    }
    if (message.eventBuffer !== 0) {
      obj.eventBuffer = Math.round(message.eventBuffer);
    }
    if (message.timeout !== undefined) {
      obj.timeout = Duration.toJSON(message.timeout);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateSessionRequest>): CreateSessionRequest {
    return CreateSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateSessionRequest>): CreateSessionRequest {
    const message = createBaseCreateSessionRequest();
    message.cookies = (globalThis.Object.entries(object.cookies ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.e2ee = object.e2ee ?? false;
    message.eventBuffer = object.eventBuffer ?? 0;
    message.timeout = (object.timeout !== undefined && object.timeout !== null)
      ? Duration.fromPartial(object.timeout)
      : undefined;
    return message;
  },
};

function createBaseCreateSessionRequest_CookiesEntry(): CreateSessionRequest_CookiesEntry {
  return { key: "", value: "" };
}

export const CreateSessionRequest_CookiesEntry: MessageFns<CreateSessionRequest_CookiesEntry> = {
  encode(message: CreateSessionRequest_CookiesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSessionRequest_CookiesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCreateSessionRequest_CookiesEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CreateSessionRequest_CookiesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: CreateSessionRequest_CookiesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateSessionRequest_CookiesEntry>): CreateSessionRequest_CookiesEntry {
    return CreateSessionRequest_CookiesEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateSessionRequest_CookiesEntry>): CreateSessionRequest_CookiesEntry {
    const message = createBaseCreateSessionRequest_CookiesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseCreateSessionResponse(): CreateSessionResponse {
  return { sessionId: "" };
}

export const CreateSessionResponse: MessageFns<CreateSessionResponse> = {
  encode(message: CreateSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCreateSessionResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sessionId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CreateSessionResponse {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: CreateSessionResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateSessionResponse>): CreateSessionResponse {
    return CreateSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateSessionResponse>): CreateSessionResponse {
    const message = createBaseCreateSessionResponse();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseConnectRequest(): ConnectRequest {
  return { sessionId: "" };
}

export const ConnectRequest: MessageFns<ConnectRequest> = {
  encode(message: ConnectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConnectRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sessionId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ConnectRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: ConnectRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<ConnectRequest>): ConnectRequest {
    return ConnectRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConnectRequest>): ConnectRequest {
    const message = createBaseConnectRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseConnectResponse(): ConnectResponse {
  return { account: undefined };
}

export const ConnectResponse: MessageFns<ConnectResponse> = {
  encode(message: ConnectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== undefined) {
      Account.encode(message.account, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConnectResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.account = Account.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ConnectResponse {
    return { account: isSet(object.account) ? Account.fromJSON(object.account) : undefined };
  },

  toJSON(message: ConnectResponse): unknown {
    const obj: any = {};
    if (message.account !== undefined) {
      obj.account = Account.toJSON(message.account);
    }
    return obj;
  },

  create(base?: DeepPartial<ConnectResponse>): ConnectResponse {
    return ConnectResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConnectResponse>): ConnectResponse {
    const message = createBaseConnectResponse();
    message.account = (object.account !== undefined && object.account !== null)
      ? Account.fromPartial(object.account)
      : undefined;
    return message;
  },
};

function createBaseCloseSessionRequest(): CloseSessionRequest {
  return { sessionId: "" };
}

export const CloseSessionRequest: MessageFns<CloseSessionRequest> = {
  encode(message: CloseSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCloseSessionRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sessionId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CloseSessionRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: CloseSessionRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<CloseSessionRequest>): CloseSessionRequest {
    return CloseSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CloseSessionRequest>): CloseSessionRequest {
    const message = createBaseCloseSessionRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseCloseSessionResponse(): CloseSessionResponse {
  return {};
}

export const CloseSessionResponse: MessageFns<CloseSessionResponse> = {
  encode(_: CloseSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCloseSessionResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): CloseSessionResponse {
    return {};
  },

  toJSON(_: CloseSessionResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CloseSessionResponse>): CloseSessionResponse {
    return CloseSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CloseSessionResponse>): CloseSessionResponse {
    const message = createBaseCloseSessionResponse();
    return message;
  },
};

function createBaseGetHealthRequest(): GetHealthRequest {
  return { sessionId: "" };
}

export const GetHealthRequest: MessageFns<GetHealthRequest> = {
  encode(message: GetHealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetHealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetHealthRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sessionId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetHealthRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: GetHealthRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetHealthRequest>): GetHealthRequest {
    return GetHealthRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetHealthRequest>): GetHealthRequest {
    const message = createBaseGetHealthRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseGetHealthResponse(): GetHealthResponse {
  return { health: undefined };
}

export const GetHealthResponse: MessageFns<GetHealthResponse> = {
  encode(message: GetHealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.health !== undefined) {
      HealthSnapshot.encode(message.health, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetHealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetHealthResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.health = HealthSnapshot.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetHealthResponse {
    return { health: isSet(object.health) ? HealthSnapshot.fromJSON(object.health) : undefined };
  },

  toJSON(message: GetHealthResponse): unknown {
    const obj: any = {};
    if (message.health !== undefined) {
      obj.health = HealthSnapshot.toJSON(message.health);
    }
    return obj;
  },

  create(base?: DeepPartial<GetHealthResponse>): GetHealthResponse {
    return GetHealthResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetHealthResponse>): GetHealthResponse {
    const message = createBaseGetHealthResponse();
    message.health = (object.health !== undefined && object.health !== null)
      ? HealthSnapshot.fromPartial(object.health)
      : undefined;
    return message;
  },
};

function createBaseSubscribeEventsRequest(): SubscribeEventsRequest {
  return { sessionId: "", buffer: 0 };
}

export const SubscribeEventsRequest: MessageFns<SubscribeEventsRequest> = {
  encode(message: SubscribeEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.buffer !== 0) {
      writer.uint32(16).uint32(message.buffer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSubscribeEventsRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.buffer = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SubscribeEventsRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      buffer: isSet(object.buffer) ? globalThis.Number(object.buffer) : 0,
    };
  },

  toJSON(message: SubscribeEventsRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.buffer !== 0) {
      obj.buffer = Math.round(message.buffer);
    }
    return obj;
  },

  create(base?: DeepPartial<SubscribeEventsRequest>): SubscribeEventsRequest {
    return SubscribeEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SubscribeEventsRequest>): SubscribeEventsRequest {
    const message = createBaseSubscribeEventsRequest();
    message.sessionId = object.sessionId ?? "";
    message.buffer = object.buffer ?? 0;
    return message;
  },
};

function createBaseSubscribeEventsResponse(): SubscribeEventsResponse {
  return { event: undefined };
}

export const SubscribeEventsResponse: MessageFns<SubscribeEventsResponse> = {
  encode(message: SubscribeEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.event !== undefined) {
      Event.encode(message.event, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSubscribeEventsResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.event = Event.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SubscribeEventsResponse {
    return { event: isSet(object.event) ? Event.fromJSON(object.event) : undefined };
  },

  toJSON(message: SubscribeEventsResponse): unknown {
    const obj: any = {};
    if (message.event !== undefined) {
      obj.event = Event.toJSON(message.event);
    }
    return obj;
  },

  create(base?: DeepPartial<SubscribeEventsResponse>): SubscribeEventsResponse {
    return SubscribeEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SubscribeEventsResponse>): SubscribeEventsResponse {
    const message = createBaseSubscribeEventsResponse();
    message.event = (object.event !== undefined && object.event !== null) ? Event.fromPartial(object.event) : undefined;
    return message;
  },
};

export type SessionServiceDefinition = typeof SessionServiceDefinition;
export const SessionServiceDefinition = {
  name: "SessionService",
  fullName: "meta.v1.SessionService",
  methods: {
    createSession: {
      name: "CreateSession",
      requestType: CreateSessionRequest as typeof CreateSessionRequest,
      requestStream: false,
      responseType: CreateSessionResponse as typeof CreateSessionResponse,
      responseStream: false,
      options: {},
    },
    connect: {
      name: "Connect",
      requestType: ConnectRequest as typeof ConnectRequest,
      requestStream: false,
      responseType: ConnectResponse as typeof ConnectResponse,
      responseStream: false,
      options: {},
    },
    closeSession: {
      name: "CloseSession",
      requestType: CloseSessionRequest as typeof CloseSessionRequest,
      requestStream: false,
      responseType: CloseSessionResponse as typeof CloseSessionResponse,
      responseStream: false,
      options: {},
    },
    getHealth: {
      name: "GetHealth",
      requestType: GetHealthRequest as typeof GetHealthRequest,
      requestStream: false,
      responseType: GetHealthResponse as typeof GetHealthResponse,
      responseStream: false,
      options: {},
    },
    subscribeEvents: {
      name: "SubscribeEvents",
      requestType: SubscribeEventsRequest as typeof SubscribeEventsRequest,
      requestStream: false,
      responseType: SubscribeEventsResponse as typeof SubscribeEventsResponse,
      responseStream: true,
      options: {},
    },
  },
} as const;

export interface SessionServiceImplementation<CallContextExt = {}> {
  createSession(
    request: CreateSessionRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<CreateSessionResponse>>;
  connect(request: ConnectRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ConnectResponse>>;
  closeSession(
    request: CloseSessionRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<CloseSessionResponse>>;
  getHealth(request: GetHealthRequest, context: CallContext & CallContextExt): Promise<DeepPartial<GetHealthResponse>>;
  subscribeEvents(
    request: SubscribeEventsRequest,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<SubscribeEventsResponse>>;
}

export interface SessionServiceClient<CallOptionsExt = {}> {
  createSession(
    request: DeepPartial<CreateSessionRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<CreateSessionResponse>;
  connect(request: DeepPartial<ConnectRequest>, options?: CallOptions & CallOptionsExt): Promise<ConnectResponse>;
  closeSession(
    request: DeepPartial<CloseSessionRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<CloseSessionResponse>;
  getHealth(request: DeepPartial<GetHealthRequest>, options?: CallOptions & CallOptionsExt): Promise<GetHealthResponse>;
  subscribeEvents(
    request: DeepPartial<SubscribeEventsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): AsyncIterable<SubscribeEventsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type ServerStreamingMethodResult<Response> = { [Symbol.asyncIterator](): AsyncIterator<Response, void> };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
