version: 2
project_name: fbgo

before:
  hooks:
    - go mod verify

builds:
  - id: fbgo
    main: ./cmd/fbgo
    binary: fbgo
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X go.mewis.me/fbgo.Version={{ .Version }} -X go.mewis.me/fbgo.Commit={{ .Commit }}
    mod_timestamp: "{{ .CommitTimestamp }}"
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: fbgo
    ids:
      - fbgo
    name_template: "fbgo_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md
      - THIRD_PARTY_LICENSES.md
      - docs/*

checksum:
  name_template: "fbgo_{{ .Version }}_checksums.txt"
  algorithm: sha256

sboms:
  - id: archives
    artifacts: archive

changelog:
  use: git
  sort: asc

release:
  draft: false
  prerelease: auto
