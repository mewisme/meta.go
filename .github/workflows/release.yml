name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: Existing release tag to resume
        required: true
        type: string
      release_only:
        description: Resume only the Go/GitHub release using artifacts from a previous run
        required: false
        default: false
        type: boolean
      artifact_run_id:
        description: Previous release workflow run ID containing runtime artifacts
        required: false
        type: string
      skip_node_runtime:
        description: Skip an already published Node runtime
        required: false
        default: false
        type: boolean
      skip_node_sdk:
        description: Skip an already published Node SDK
        required: false
        default: false
        type: boolean
      skip_python_runtime:
        description: Skip an already published Python runtime
        required: false
        default: false
        type: boolean
      skip_python_sdk:
        description: Skip an already published Python SDK
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  GOWORK: off

jobs:
  validate:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - id: version
        name: Resolve release version
        env:
          RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
        run: echo "version=$(bash scripts/release-version.sh "$RELEASE_TAG")" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-go@v7
        if: github.event_name != 'workflow_dispatch' || !inputs.release_only
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/setup-node@v7
        if: github.event_name != 'workflow_dispatch' || !inputs.release_only
        with:
          node-version: 24
      - uses: astral-sh/setup-uv@v10.0.1
        if: github.event_name != 'workflow_dispatch' || !inputs.release_only
        with:
          version: "0.12.10"
      - name: Verify source
        if: github.event_name != 'workflow_dispatch' || !inputs.release_only
        run: |
          corepack enable
          corepack prepare pnpm@11.25.0 --activate
          go mod verify
          test -z "$(gofmt -l .)"
          go vet ./...
          go run honnef.co/go/tools/cmd/staticcheck@latest ./...
          go test ./...
          go test -race ./...
          go mod tidy
          git diff --exit-code -- go.mod go.sum
          go run golang.org/x/vuln/cmd/govulncheck@latest ./...
          pnpm -C sdk/node install --frozen-lockfile
          pnpm -C sdk/node check
          uv sync --directory sdk/python --frozen --all-groups
          uv run --directory sdk/python ruff check src/mewisme_meta tests scripts runtime/src
          uv run --directory sdk/python ruff format --check src/mewisme_meta tests scripts runtime/src
          uv run --directory sdk/python mypy src/mewisme_meta
          uv run --directory sdk/python pytest

  build-runtime:
    if: github.event_name != 'workflow_dispatch' || !inputs.release_only
    needs: validate
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: linux-x64
            goos: linux
            goarch: amd64
            ext: ""
          - id: linux-arm64
            goos: linux
            goarch: arm64
            ext: ""
          - id: darwin-x64
            goos: darwin
            goarch: amd64
            ext: ""
          - id: darwin-arm64
            goos: darwin
            goarch: arm64
            ext: ""
          - id: win32-x64
            goos: windows
            goarch: amd64
            ext: .exe
          - id: win32-arm64
            goos: windows
            goarch: arm64
            ext: .exe
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - name: Install UPX
        shell: bash
        run: |
          curl -fsSL https://github.com/upx/upx/releases/download/v5.2.1/upx-5.2.1-amd64_linux.tar.xz | tar -xJ -C "$RUNNER_TEMP"
          echo "$RUNNER_TEMP/upx-5.2.1-amd64_linux" >> "$GITHUB_PATH"
      - name: Build runtime
        env:
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          mkdir -p runtime
          output="runtime/meta-runtime_${VERSION}_${{ matrix.goos }}_${{ matrix.goarch }}${{ matrix.ext }}"
          CGO_ENABLED=0 GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -trimpath -ldflags "-s -w -X go.mewis.me/meta.go.Version=${VERSION} -X go.mewis.me/meta.go.Commit=${GITHUB_SHA}" -o "$output" ./cmd/meta-runtime
          case "${{ matrix.goos }}/${{ matrix.goarch }}" in
            linux/amd64|linux/arm64|windows/amd64)
              upx --best "$output"
              upx -t "$output"
              ;;
          esac
          test -s "$output"
      - uses: actions/upload-artifact@v7
        with:
          name: runtime-${{ matrix.id }}
          path: runtime/*
          if-no-files-found: error

  package-node-runtime:
    needs: [validate, build-runtime]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [linux-x64, linux-arm64, darwin-x64, darwin-arm64, win32-x64, win32-arm64]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - uses: actions/download-artifact@v8
        with:
          name: runtime-${{ matrix.target }}
          path: runtime
      - name: Package Node runtime
        env:
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          corepack enable
          corepack prepare pnpm@11.25.0 --activate
          binary=$(find runtime -type f -name 'meta-runtime*' -print -quit)
          node sdk/node/scripts/package-runtime.mjs --target "${{ matrix.target }}" --version "$VERSION" --binary "$binary" --out-dir dist
      - uses: actions/upload-artifact@v7
        with:
          name: node-runtime-${{ matrix.target }}
          path: dist/*.tgz
          if-no-files-found: error

  package-python-runtime:
    needs: [validate, build-runtime]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-x64-manylinux
            runtime: linux-x64
          - target: linux-x64-musllinux
            runtime: linux-x64
          - target: linux-arm64-manylinux
            runtime: linux-arm64
          - target: linux-arm64-musllinux
            runtime: linux-arm64
          - target: darwin-x64
            runtime: darwin-x64
          - target: darwin-arm64
            runtime: darwin-arm64
          - target: win32-x64
            runtime: win32-x64
          - target: win32-arm64
            runtime: win32-arm64
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          name: runtime-${{ matrix.runtime }}
          path: runtime
      - name: Package Python runtime
        env:
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          binary=$(find runtime -type f -name 'meta-runtime*' -print -quit)
          python3 sdk/python/scripts/package-runtime.py --target "${{ matrix.target }}" --version "$VERSION" --binary "$binary" --out-dir dist
      - uses: actions/upload-artifact@v7
        with:
          name: python-runtime-${{ matrix.target }}
          path: dist/*.whl
          if-no-files-found: error

  package-node-sdk:
    if: github.event_name != 'workflow_dispatch' || !inputs.release_only
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Package Node SDK
        env:
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          corepack enable
          corepack prepare pnpm@11.25.0 --activate
          pnpm -C sdk/node install --frozen-lockfile
          node sdk/node/scripts/prepare-release.mjs "$VERSION"
          python3 scripts/validate-release.py --version "$VERSION" --node-dir sdk/node
          mkdir -p dist
          pnpm -C sdk/node pack --pack-destination ../../dist --silent
          python3 scripts/validate-release.py --version "$VERSION" --node-sdk-archive "$(find dist -name '*.tgz' -print -quit)"
      - uses: actions/upload-artifact@v7
        with:
          name: node-sdk
          path: dist/*.tgz
          if-no-files-found: error

  package-python-sdk:
    if: github.event_name != 'workflow_dispatch' || !inputs.release_only
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v10.0.1
        with:
          version: "0.12.10"
      - name: Package Python SDK
        env:
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          uv sync --directory sdk/python --frozen --all-groups
          python3 sdk/python/scripts/prepare-release.py "$VERSION"
          python3 scripts/validate-release.py --version "$VERSION" --python-dir sdk/python
          uv build --directory sdk/python --out-dir "$GITHUB_WORKSPACE/dist"
          python3 scripts/validate-release.py --version "$VERSION" --python-sdk-wheel "$(find dist -name '*.whl' -print -quit)"
      - uses: actions/upload-artifact@v7
        with:
          name: python-sdk
          path: dist/*
          if-no-files-found: error

  validate-node-runtime-artifacts:
    needs: [validate, package-node-runtime]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          pattern: node-runtime-*
          path: dist
          merge-multiple: true
      - env:
          VERSION: ${{ needs.validate.outputs.version }}
        run: python3 scripts/validate-release.py --version "$VERSION" --node-runtime-dir dist

  validate-python-runtime-artifacts:
    needs: [validate, package-python-runtime]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          pattern: python-runtime-*
          path: dist
          merge-multiple: true
      - env:
          VERSION: ${{ needs.validate.outputs.version }}
        run: python3 scripts/validate-release.py --version "$VERSION" --python-runtime-dir dist

  publish-node-runtime:
    if: github.event_name != 'workflow_dispatch' || !inputs.skip_node_runtime
    needs: [validate, validate-node-runtime-artifacts]
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - uses: actions/download-artifact@v8
        with:
          pattern: node-runtime-*
          path: dist
          merge-multiple: true
      - run: for package in ./dist/*.tgz; do npm publish "$package" --access public --provenance; done

  publish-python-runtime:
    if: github.event_name != 'workflow_dispatch' || !inputs.skip_python_runtime
    needs: validate-python-runtime-artifacts
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: python-runtime-*
          path: dist
          merge-multiple: true
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist

  publish-node-sdk:
    if: always() && needs.package-node-sdk.result == 'success' && (needs.publish-node-runtime.result == 'success' || (github.event_name == 'workflow_dispatch' && inputs.skip_node_runtime)) && !(github.event_name == 'workflow_dispatch' && inputs.skip_node_sdk)
    needs: [publish-node-runtime, package-node-sdk]
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - uses: actions/download-artifact@v8
        with:
          name: node-sdk
          path: dist
      - run: npm publish "./$(find dist -name '*.tgz' -print -quit)" --access public --provenance

  publish-python-sdk:
    if: always() && needs.validate.result == 'success' && needs.package-python-sdk.result == 'success' && (needs.publish-python-runtime.result == 'success' || (github.event_name == 'workflow_dispatch' && inputs.skip_python_runtime)) && !(github.event_name == 'workflow_dispatch' && inputs.skip_python_sdk)
    needs: [validate, package-python-sdk, publish-python-runtime]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: python-sdk
          path: dist
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist

  release-go:
    if: always() && needs.validate.result == 'success' && ((github.event_name == 'workflow_dispatch' && inputs.release_only) || (needs.build-runtime.result == 'success' && (needs.publish-node-sdk.result == 'success' || (github.event_name == 'workflow_dispatch' && inputs.skip_node_sdk)) && (needs.publish-python-sdk.result == 'success' || (github.event_name == 'workflow_dispatch' && inputs.skip_python_sdk))))
    needs: [validate, build-runtime, publish-node-sdk, publish-python-sdk]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
      attestations: write
      artifact-metadata: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_only && inputs.tag || github.ref }}
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/download-artifact@v8
        with:
          pattern: runtime-*
          path: ${{ runner.temp }}/runtime-dist
          merge-multiple: true
          run-id: ${{ github.event_name == 'workflow_dispatch' && inputs.release_only && inputs.artifact_run_id || github.run_id }}
          repository: ${{ github.repository }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Runtime checksums
        env:
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          cd "$RUNNER_TEMP/runtime-dist"
          sha256sum meta-runtime_* > "meta-runtime_${VERSION}_checksums.txt"
      - name: Install Syft
        id: syft
        uses: anchore/sbom-action/download-syft@v0.24.2
      - name: Add Syft to PATH
        run: dirname '${{ steps.syft.outputs.cmd }}' >> "$GITHUB_PATH"
      - name: Install UPX
        shell: bash
        run: |
          curl -fsSL https://github.com/upx/upx/releases/download/v5.2.1/upx-5.2.1-amd64_linux.tar.xz | tar -xJ -C "$RUNNER_TEMP"
          echo "$RUNNER_TEMP/upx-5.2.1-amd64_linux" >> "$GITHUB_PATH"
      - name: Release Go CLI
        uses: goreleaser/goreleaser-action@v7
        with:
          distribution: goreleaser
          version: v2.18.1
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload standalone runtime assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
        run: gh release upload "$RELEASE_TAG" "$RUNNER_TEMP"/runtime-dist/* --clobber
      - name: Attest release artifacts
        uses: actions/attest@v4
        with:
          subject-path: |
            dist/*.tar.gz
            dist/*.zip
            dist/*checksums.txt
            dist/*.sbom.json
            ${{ runner.temp }}/runtime-dist/*
